CRM_DATABASE_URL=postgresql+psycopg://crm:crm@db:5432/crm

# Attachment storage. Relative paths resolve against the backend working
# directory; the tree is created on first upload.
CRM_ATTACHMENTS_DIR=./var/attachments
CRM_MAX_UPLOAD_BYTES=10485760
